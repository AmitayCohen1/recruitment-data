"use client";

import { CombatParadox } from "./combat-paradox";

/** "Why it differs": keeps the rate-vs-volume contrast after absolute
 *  contribution moved into the 2018-vs-2024 composition card. */
export function GapsOverview() {
  return <CombatParadox />;
}
